# ─── Server ──────────────────────────────────────────────
PORT=3010
NODE_ENV=development

# ─── Database ─────────────────────────────────────────────
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=nfc_attendance

# ─── JWT（App 自签）──────────────────────────────────────
JWT_SECRET=change_this_to_a_strong_random_secret

# ─── Supabase 配置 ────────────────────────────────────────
# Dashboard → Settings → API → Project URL
SUPABASE_URL=https://xmbwbjqezgrybpfbtqba.supabase.co

# Dashboard → Settings → API → Project API keys → anon / publishable
SUPABASE_ANON_KEY=sb_publishable_u6vCC2KS4lD2qKIdb5lhQg_s1DZ_721

# Dashboard → Settings → API → JWT Settings → JWT Secret（后端专用，勿泄露）
SUPABASE_JWT_SECRET=your-supabase-jwt-secret

# ─── NFC ──────────────────────────────────────────────────
NFC_DOMAIN=nfc.meizu.life
